<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark"
    android:gravity="center"
    tools:context=".MainActivity">

    <ImageView
        android:layout_width="@dimen/imageAuthSize"
        android:layout_height="@dimen/imageAuthSize"
        android:src="@drawable/ic_icon_auth"/>

    <TextView
        android:id="@+id/authMain"
        style="@style/TextStyle"
        android:text="@string/app_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/authMainMarginTop"
        android:layout_marginBottom="@dimen/authMainMarginBottom"
        />

    <android.support.design.button.MaterialButton
        android:id="@+id/enterButton"
        app:cornerRadius="@dimen/ButtonCornerRadius"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimaryDark"
        android:textSize="@dimen/ButtonTextSize"
        android:textColor="@color/colorPrimaryDark"
        android:text="@string/enterButtonTitle"
        android:paddingStart="@dimen/ButtonPaddingStartEnd"
        android:paddingEnd="@dimen/ButtonPaddingStartEnd"
        android:paddingTop="@dimen/ButtonPaddingTopBottom"
        android:paddingBottom="@dimen/ButtonPaddingTopBottom"/>

</LinearLayout>